// a new app that connects to an api: mealdb: we loop through the meal coming back and display it in ul
import{useEffect} from "react"

const Meal = () =>{
    useEffect(()=>{
        console.log("Hello world")
    })
    return (<div>Meal</div>)
}

export default Meal